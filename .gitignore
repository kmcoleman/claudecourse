__pycache__/
*.py[cod]
.venv/
venv/
*.egg-info/
.pytest_cache/
build/
dist/
.DS_Store
.superpowers/

# Kit secrets — never commit the real key (.env.example is committed)
.env

# Both quarters are committed course data: Q3 is the quarter students review,
# Q4 ships pre-generated for the Day 5 transfer lesson. Do NOT ignore /data.

# Learner-produced artifacts (reconciliation + review outputs)
/identities.csv
/ledger.csv
/findings*.json
/exemptions*.json
/audit/
